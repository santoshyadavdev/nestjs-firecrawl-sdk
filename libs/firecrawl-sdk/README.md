# firecrawl-sdk

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build firecrawl-sdk` to build the library.

## Running unit tests

Run `nx test firecrawl-sdk` to execute the unit tests via [Vitest](https://vitest.dev/).
